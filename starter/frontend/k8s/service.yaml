apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  type: LoadBalancer   # <-- Must be LoadBalancer, not NodePort
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000